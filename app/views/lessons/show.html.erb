<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_lesson_path(@lesson), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @lesson.title %></h2>
  <div class="fr-view"><%= @lesson.content.html_safe %></div>
</div>